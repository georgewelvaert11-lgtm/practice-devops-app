apiVersion: v1
kind: Service
metadata:
  labels:
    app: practice-devops-app-service
  name: practice-devops-app-service
spec:
  ports:
  - name: 5000-5000
    port: 5000
    protocol: TCP
    targetPort: 5000
  selector:
    app: practice-devops-app
  type: NodePort
